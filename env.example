# UniMem AI - 环境变量配置示例
# 复制此文件为 .env 并填入您的实际配置

# ===========================================
# NVIDIA NIM API 配置
# ===========================================
# 获取方式：https://build.nvidia.com/
NVIDIA_API_KEY=your_nvidia_api_key_here

# API基础URL（开发环境）
NVIDIA_API_BASE_URL=https://integrate.api.nvidia.com/v1

# 生产环境NIM URL（如果使用NIM）
NIM_EMBEDDING_URL=https://your-nim-instance.nvcr.io/v1

# ===========================================
# AWS 配置
# ===========================================
# 获取方式：AWS IAM控制台
AWS_ACCESS_KEY_ID=your_aws_access_key_id_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key_here

# AWS区域
AWS_REGION=us-east-1

# S3存储桶名称（需要先创建）
S3_BUCKET_NAME=your-unimem-uploads-bucket

# ===========================================
# 应用配置
# ===========================================
# 环境：development, staging, production
ENVIRONMENT=development

# 嵌入模型
EMBEDDING_MODEL=nvidia/llama-3.2-nv-embedqa-1b-v2

# 允许的文件扩展名
ALLOWED_EXTENSIONS=pdf,docx,txt,md,jpg,jpeg,png,gif

# ===========================================
# 数据库配置
# ===========================================
# DynamoDB表名前缀
MEMORIES_TABLE_PREFIX=unimem-memories
EMBEDDINGS_TABLE_PREFIX=unimem-embeddings

# ===========================================
# 服务器配置
# ===========================================
# 服务器端口
PORT=8012

# 服务器主机
HOST=0.0.0.0

# ===========================================
# 安全配置
# ===========================================
# 用于加密的密钥（生产环境必须更改）
SECRET_KEY=your-secret-key-here-change-in-production

# JWT算法
ALGORITHM=HS256

# 访问令牌过期时间（分钟）
ACCESS_TOKEN_EXPIRE_MINUTES=30

# 刷新令牌过期时间（天）
REFRESH_TOKEN_EXPIRE_DAYS=7

# ===========================================
# 日志配置
# ===========================================
# 日志级别：DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# 日志文件路径
LOG_FILE=logs/unimem.log

# ===========================================
# 性能配置
# ===========================================
# 最大文件大小（字节）
MAX_FILE_SIZE=10485760  # 10MB

# 最大文本长度（字符）
MAX_TEXT_LENGTH=100000

# 嵌入向量维度
EMBEDDING_DIMENSION=2048

# ===========================================
# 开发配置
# ===========================================
# 是否启用调试模式
DEBUG=true

# 是否启用热重载
RELOAD=true

# 是否启用CORS
CORS_ENABLED=true

# ===========================================
# 注意事项
# ===========================================
# 1. 请勿将包含真实密钥的 .env 文件提交到版本控制
# 2. 生产环境请使用强密码和安全的密钥
# 3. 定期轮换API密钥和访问凭证
# 4. 使用环境变量或密钥管理服务存储敏感信息
# 5. 确保AWS用户具有最小必要权限
